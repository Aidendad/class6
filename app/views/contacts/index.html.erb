<h1>contacts</h1>

<% contacts = Contact.all %>

<ul>
  <% for contact in contacts %>
    <li>
    <%= contact["first name"] %><%= contact["last name"] %>
    <% Company.find_by({"id" => contact["company_id"]}) %>
    (<%= company["name"] %>)
    </li>

  <% end %>
</ul>